import { View } from "react-native";

// This file is just a placeholder to make the Tab Bar happy.
// We will never actually see this screen.
export default function CreatePlaceholder() {
  return <View />;
}
